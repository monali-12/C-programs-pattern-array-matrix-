#include<stdio.h>
#include<string.h>
int  main()
{
	char str[100];
	int i,n,r;
	printf("enter the string\n");
	scanf("%s",&str);
	for(i=0;str[i]!='\0';i++);
	n=i;
	r=n;
	printf("the length of %s is %d\n",str,n);
	printf("\nfind the no of permutation of given string\n");
	
	
	
}
